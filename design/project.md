
create a python project for status site generation

it should do the following:

* when run it should render all the markdown files in the folder docs as html into the folder output in the same
* it should use the jinja2 file src/templates/posts as the default layout for the list of posts  
* it shold use the  jinja2 file src/templates/page as the default layout for a makrdown file
* it should use tailwindcss

create a sample dir called example with the structure above with sample posts
